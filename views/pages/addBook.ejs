<% include ../layouts/header %>
<p>This is new book form</p>
<form action="/admin/mainPage/addBook" method="POST">
    Book name:<br>
    <input type="text" name="bookName" value="Pride and Prejuistice">
    <br><br>
    Category<br>
    <select name="categorySelect">
      <% categories.forEach( (category) => { %>
      <option value=<%= category._id %>><%= category.categoryName%></option>
      <% }) %>
    </select>
    <br><br>
    Author name:<br>
    <select name="authorSelect">
      <% authors.forEach( (author) => { %>
      <option value=<%= author._id %>><%= author.authorName.first %></option>
      <% }) %>
    </select>
    <br><br>
    <input type="submit" value="Submit">
  </form> 
  <% include ../layouts/footer %>